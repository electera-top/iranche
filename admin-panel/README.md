# پنل مدیریت ایرانچه

## 📁 ساختار فایل‌ها

### فایل‌های JavaScript (پوشه `js/`):
- `js/common.js` - توابع مشترک و helper functions
- `js/notifications.js` - سیستم اعلان‌ها
- `js/sidebar.js` - مدیریت sidebar مشترک
- `js/script.js` - اسکریپت اصلی داشبورد

### فایل‌های HTML:
- `index.html` - صفحه اصلی داشبورد
- `categories.html` - مدیریت دسته‌بندی فروشگاه‌ها
- `files.html` - مدیریت فایل‌ها

### فایل‌های CSS (پوشه `css/`):
- `css/styles.css` - استایل‌های مشترک

### فایل‌های مشترک:
- `sidebar.html` - HTML sidebar مشترک
- `header.html` - HTML header مشترک
- `loading.html` - HTML loading overlay

## 🚀 نحوه استفاده

### 1. اجرای سرور محلی:
```bash
cd admin-panel
python -m http.server 8080
```

### 2. دسترسی:
- **داشبورد**: `http://localhost:8080/index.html`
- **دسته‌بندی‌ها**: `http://localhost:8080/categories.html`
- **فایل‌ها**: `http://localhost:8080/files.html`

## 🔧 ویژگی‌ها

### سیستم مشترک:
- **Sidebar یکپارچه**: در تمام صفحات
- **اعلان‌های مشترک**: success, error, warning, info
- **Loading overlay**: نمایش بارگذاری
- **API Helper**: توابع مشترک برای API calls

### مدیریت فایل‌ها:
- **آپلود**: با Dropzone (drag & drop)
- **دسته‌بندی**: 9 دسته مختلف
- **فیلتر**: بر اساس نوع و دسته‌بندی
- **جستجو**: در نام فایل‌ها

### مدیریت دسته‌بندی‌ها:
- **طبقات**: با بازه عددی
- **زیردسته‌ها**: نمایش درختی
- **مدیریت**: اضافه، ویرایش، حذف

## 📊 API Endpoints

### فایل‌ها:
- `GET /api/files` - لیست فایل‌ها
- `POST /api/files/upload` - آپلود فایل
- `DELETE /api/files/:id` - حذف فایل
- `GET /api/files/categories` - دسته‌بندی‌ها

### دسته‌بندی‌ها:
- `GET /api/categories/floors` - لیست طبقات
- `POST /api/categories/floors` - ایجاد طبقه
- `GET /api/categories/categories` - لیست زیردسته‌ها
- `POST /api/categories/categories` - ایجاد زیردسته

## 🎨 استایل‌ها

### رنگ‌بندی:
- **Primary**: #3B82F6 (آبی)
- **Success**: #10B981 (سبز)
- **Error**: #EF4444 (قرمز)
- **Warning**: #F59E0B (نارنجی)

### فونت:
- **Vazirmatn**: فونت فارسی
- **Font Awesome**: آیکون‌ها

## 🔄 به‌روزرسانی

برای اضافه کردن صفحه جدید:
1. فایل HTML ایجاد کنید
2. `common.js`, `notifications.js`, `sidebar.js` را import کنید
3. از توابع مشترک استفاده کنید

## 📝 نکات مهم

- تمام API calls از `adminCommon.apiCall()` استفاده می‌کنند
- اعلان‌ها از `showNotification()` استفاده می‌کنند
- Loading از `showLoading()` و `hideLoading()` استفاده می‌کند
- Sidebar به صورت خودکار active state را تنظیم می‌کند