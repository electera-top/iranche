"use client";

// این کامپوننت دیگر استفاده نمی‌شود و توسط FloatingMenu جایگزین شده است
// پارامترهای isOpen و onClose برای سازگاری با API قبلی حفظ شده‌اند
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function TopMoreMenu({ isOpen, onClose }: { isOpen: boolean; onClose: () => void }) {
  // بدنه خالی - این کامپوننت دیگر عملکردی ندارد
  return null;
} 