# 🔌 راهنمای اتصال به سرور

## ⚠️ مشکل اتصال

اتصال خودکار به سرور با خطا مواجه شد:
```
FATAL ERROR: Network error: Connection timed out
```

این یعنی:
- ممکن است پورت SSH اشتباه باشد
- فایروال سرور اتصال را بلاک می‌کند
- IP یا اطلاعات سرور نیاز به بررسی دارد

---

## ✅ راه‌حل‌ها

### 1️⃣ بررسی پورت SSH صحیح

پورت SSH معمولاً **22** است، نه 12111002. احتمالاً:
- **12111002** شماره پسورد است (نه پورت!)
- یا شماره یک پورت خاص داخلی است

**اول این را امتحان کنید:**

```powershell
ssh root@178.239.147.72
```

اگر از password پرسید، عدد `12111002` را وارد کنید.

---

### 2️⃣ اگر پورت واقعاً غیر استاندارد است

اگر مطمئن هستید پورت 12111002 درست است:

```powershell
ssh -p 12111002 root@178.239.147.72
```

---

### 3️⃣ استفاده از PuTTY (راحت‌ترین روش برای ویندوز)

1. **دانلود PuTTY:**
   - برو به: https://www.putty.org/
   - دانلود کن: `putty.exe`

2. **باز کردن PuTTY:**
   - **Host Name (or IP address):** `178.239.147.72`
   - **Port:** `22` (اول این را امتحان کن)
   - **Connection type:** SSH
   - کلیک روی **Open**

3. **ورود:**
   - **login as:** `root`
   - **password:** `12111002` (یا password واقعی)

---

### 4️⃣ بررسی اینکه آیا سرور فعال است

در PowerShell:

```powershell
# تست ping
ping 178.239.147.72

# تست پورت 22
Test-NetConnection -ComputerName 178.239.147.72 -Port 22

# تست پورت 12111002
Test-NetConnection -ComputerName 178.239.147.72 -Port 12111002
```

اگر ping جواب داد اما SSH نه، یعنی فایروال پورت SSH را بسته.

---

### 5️⃣ اگر فایروال مشکل دارد

باید از پنل مدیریت سرور (مثل cPanel, Plesk, یا پنل hosting provider) پورت SSH را باز کنید:

1. وارد پنل مدیریت سرور شوید
2. بخش **Firewall** یا **Security** را پیدا کنید
3. پورت **22** را برای SSH باز کنید
4. یا پورت custom (اگر دارید) را باز کنید

---

## 📝 بعد از اتصال موفق

وقتی توانستید به سرور متصل شوید، این دستورات را اجرا کنید:

```bash
# 1. نصب Git
dnf install -y git

# 2. کلون پروژه
cd /root
git clone https://github.com/electera-top/iranche.git
cd iranche/deploy

# 3. اجرای deploy
chmod +x *.sh
./full-deploy.sh
```

یا اگر می‌خواهید مرحله به مرحله:

```bash
# مرحله 1: نصب نیازمندی‌ها
./server-setup.sh

# مرحله 2: deploy برنامه
./deploy-app.sh

# مرحله 3: تنظیم nginx
./setup-nginx.sh

# مرحله 4: SSL (اختیاری)
./setup-ssl.sh
```

---

## 🔍 عیب‌یابی اتصال

### اگر "Connection refused" می‌گیرید:
- سرویس SSH فعال نیست یا روی پورت دیگری است
- در سرور: `systemctl status sshd`

### اگر "Connection timed out" می‌گیرید:
- فایروال اتصال را بلاک می‌کند
- IP یا پورت اشتباه است

### اگر "Permission denied" می‌گیرید:
- Username یا password اشتباه است
- احتمالاً username `root` نیست

### اگر "Too many authentication failures":
- SSH config مشکل دارد
- استفاده کنید از: `ssh -o PubkeyAuthentication=no root@178.239.147.72`

---

## 💡 نکات مهم

1. **پورت 12111002 به احتمال زیاد PASSWORD است، نه PORT**
   - پورت SSH معمولاً: 22, 2222, یا 10022 است

2. **اگر سرور از یک provider خاص است:**
   - به پنل مدیریت آن provider بروید
   - دنبال "SSH Access" یا "Console" بگردید
   - ممکن است یک web-based SSH terminal داشته باشد

3. **اگر VPS تازه راه‌اندازی شده:**
   - ممکن است چند دقیقه طول بکشد تا کاملاً آماده شود
   - صبر کنید 5-10 دقیقه و دوباره امتحان کنید

---

## 📧 اطلاعات لازم برای عیب‌یابی

اگر همچنان نمی‌توانید متصل شوید، این اطلاعات را بررسی کنید:

- ✅ IP سرور: 178.239.147.72
- ❓ پورت SSH: ؟؟؟ (معمولاً 22)
- ✅ Username: root
- ❓ Password: ؟؟؟ (احتمالاً 12111002)
- ❓ Provider سرور: ؟؟؟
- ❓ آیا پنل مدیریت دارید؟ (cPanel, Plesk, etc.)

---

**توصیه:** ساده‌ترین راه این است که با PuTTY متصل شوید:
1. دانلود PuTTY
2. IP: `178.239.147.72`
3. Port: `22`
4. Login با `root` و password
5. اگر کار نکرد، پورت‌های دیگر را امتحان کنید: 2222, 10022

موفق باشید! 🚀

