# راهنمای سریع دیپلوی - iranche.com

## دستورات سریع (کپی و پیست کنید)

### 1️⃣ اتصال به سرور

```bash
ssh -p 12111002 root@178.239.147.72
```

### 2️⃣ نصب Git (اگر نیست)

```bash
dnf install -y git
```

### 3️⃣ دانلود پروژه و رفتن به پوشه deploy

```bash
cd /root
git clone https://github.com/electera-top/iranche.git
cd iranche/deploy
chmod +x *.sh
```

### 4️⃣ اجرای دیپلوی کامل (تمام مراحل به صورت خودکار)

```bash
./full-deploy.sh
```

یا اگر ترجیح می‌دهید مرحله به مرحله اجرا کنید:

```bash
# مرحله 1: نصب نیازمندی‌ها (5-10 دقیقه)
./server-setup.sh

# مرحله 2: دیپلوی برنامه (3-5 دقیقه)
./deploy-app.sh

# مرحله 3: تنظیم Nginx
./setup-nginx.sh

# مرحله 4: تنظیم SSL (اختیاری - فقط اگر DNS آماده است)
./setup-ssl.sh
```

---

## ✅ چک لیست قبل از دیپلوی

- [ ] DNS دامنه iranche.com به IP سرور (178.239.147.72) اشاره کند
- [ ] پورت‌های 80 و 443 در فایروال/پنل سرور باز باشند
- [ ] دسترسی SSH به سرور داشته باشید

---

## 🔍 بررسی وضعیت بعد از دیپلوی

```bash
# بررسی وضعیت برنامه
pm2 status

# مشاهده لاگ‌ها
pm2 logs iranche-frontend

# تست محلی
curl http://localhost:3000

# بررسی Nginx
systemctl status nginx

# تست از خارج سرور
curl http://iranche.com
```

---

## 🔄 به‌روزرسانی برنامه

برای دریافت آخرین تغییرات و به‌روزرسانی:

```bash
cd /root/iranche/deploy
./deploy-app.sh
```

---

## 🚨 عیب‌یابی سریع

### برنامه اجرا نمی‌شود؟

```bash
pm2 logs iranche-frontend --err
cd /var/www/iranche/public-frontend
npm start  # اجرای دستی برای دیباگ
```

### Nginx خطا می‌دهد؟

```bash
nginx -t  # تست تنظیمات
tail -f /var/log/nginx/error.log  # مشاهده خطاها
```

### پورت اشغال است؟

```bash
netstat -tlnp | grep 3000
# یا
lsof -i :3000
```

---

## 📞 نکات مهم

1. **DNS Propagation:** بعد از تنظیم DNS، ممکن است 24-48 ساعت طول بکشد تا در سراسر جهان propagate شود.

2. **SSL Setup:** فقط زمانی SSL را setup کنید که مطمئن هستید DNS به درستی کار می‌کند. می‌توانید با `dig iranche.com` یا `nslookup iranche.com` بررسی کنید.

3. **Backup:** همیشه قبل از تغییرات مهم، از تنظیمات backup بگیرید:
   ```bash
   cp -r /var/www/iranche /root/backup-$(date +%Y%m%d)
   ```

4. **Security:** بعد از دیپلوی، حتما پسورد root را تغییر دهید و SSH key authentication را فعال کنید.

---

## 📧 تماس و پشتیبانی

اگر مشکلی پیش آمد، لاگ‌های زیر را بررسی کنید:
- PM2: `pm2 logs`
- Nginx: `/var/log/nginx/error.log`
- System: `journalctl -xe`

