# 🚀 شروع دیپلوی iranche.com

## مرحله 1: آماده‌سازی (از ویندوز شما)

### گزینه A: دیپلوی خودکار (توصیه می‌شود)

دستور زیر را در PowerShell اجرا کنید:

```powershell
cd D:\projects\iranche-test\shop_w2\deploy
.\deploy-from-windows.ps1
```

این اسکریپت:
- ✅ به سرور متصل می‌شود
- ✅ Git را نصب می‌کند
- ✅ پروژه را کلون می‌کند
- ✅ تمام نیازمندی‌ها را نصب می‌کند (Node.js, Nginx, PM2)
- ✅ برنامه را build و deploy می‌کند
- ✅ Nginx را تنظیم می‌کند

### گزینه B: دیپلوی دستی

اگر ترجیح می‌دهید مرحله به مرحله خودتان اجرا کنید:

```powershell
cd D:\projects\iranche-test\shop_w2\deploy
.\deploy-manual.ps1
```

سپس در سرور دستورات زیر را اجرا کنید:

```bash
dnf install -y git
cd /root
git clone https://github.com/electera-top/iranche.git
cd iranche/deploy
chmod +x *.sh
./full-deploy.sh
```

---

## مرحله 2: بررسی وضعیت

بعد از دیپلوی، سایت شما در آدرس زیر در دسترس است:

```
http://iranche.com
```

برای بررسی وضعیت برنامه روی سرور:

```bash
pm2 status
pm2 logs iranche-frontend
```

---

## مرحله 3: تنظیم HTTPS (اختیاری اما توصیه می‌شود)

⚠️ **مهم:** قبل از این مرحله، مطمئن شوید DNS دامنه به سرور اشاره می‌کند.

برای بررسی DNS:

```bash
dig iranche.com
nslookup iranche.com
```

اگر DNS آماده است، روی سرور دستور زیر را اجرا کنید:

```bash
cd /root/iranche/deploy
./setup-ssl.sh
```

بعد از نصب SSL، سایت در آدرس زیر هم در دسترس خواهد بود:

```
https://iranche.com
```

---

## 📋 اطلاعات سرور

- **IP:** 178.239.147.72
- **Port SSH:** 12111002
- **User:** root
- **Domain:** iranche.com

---

## 🔧 دستورات مفید

### مدیریت برنامه

```bash
pm2 restart iranche-frontend    # ری‌استارت برنامه
pm2 stop iranche-frontend       # توقف برنامه
pm2 logs iranche-frontend       # مشاهده لاگ‌ها
pm2 monit                        # مانیتورینگ زنده
```

### به‌روزرسانی برنامه

```bash
cd /root/iranche/deploy
./deploy-app.sh
```

### مدیریت Nginx

```bash
systemctl status nginx          # بررسی وضعیت
systemctl restart nginx         # ری‌استارت
nginx -t                        # تست تنظیمات
tail -f /var/log/nginx/error.log   # مشاهده خطاها
```

---

## 🆘 عیب‌یابی

### برنامه کار نمی‌کند؟

```bash
# بررسی لاگ‌ها
pm2 logs iranche-frontend --err

# اجرای دستی برای دیباگ
cd /var/www/iranche/public-frontend
npm run build
npm start
```

### Nginx خطا می‌دهد؟

```bash
# بررسی تنظیمات
nginx -t

# مشاهده خطاها
tail -f /var/log/nginx/error.log

# بررسی وضعیت
systemctl status nginx
```

### پورت 3000 اشغال است؟

```bash
# پیدا کردن پروسس
lsof -i :3000

# یا
netstat -tlnp | grep 3000

# کشتن پروسس
kill -9 <PID>
```

---

## 📁 ساختار فایل‌های دیپلوی

```
deploy/
├── START-HERE.md              # 👈 همین فایل
├── quick-deploy.md            # راهنمای سریع
├── README.md                  # راهنمای کامل
├── deploy-from-windows.ps1    # اسکریپت خودکار ویندوز
├── deploy-manual.ps1          # اتصال SSH ساده
├── full-deploy.sh             # دیپلوی کامل (Linux)
├── server-setup.sh            # نصب نیازمندی‌ها
├── deploy-app.sh              # دیپلوی برنامه
├── setup-nginx.sh             # تنظیم Nginx
├── setup-ssl.sh               # تنظیم SSL
└── nginx-iranche.conf         # فایل کانفیگ Nginx
```

---

## ✅ چک لیست

قبل از شروع، مطمئن شوید:

- [ ] DNS دامنه iranche.com به IP 178.239.147.72 اشاره کند
- [ ] پورت‌های 80 و 443 باز باشند
- [ ] دسترسی SSH به سرور داشته باشید
- [ ] حداقل 2GB RAM و 10GB دیسک آزاد داشته باشید

---

## 🎉 بعد از دیپلوی موفق

سایت شما آماده است! موارد زیر را انجام دهید:

1. ✅ سایت را در مرورگر باز کنید: http://iranche.com
2. ✅ SSL را تنظیم کنید (اگر DNS آماده است)
3. ✅ پسورد root سرور را تغییر دهید
4. ✅ از تنظیمات و دیتابیس backup بگیرید
5. ✅ مانیتورینگ و لاگ‌ها را راه‌اندازی کنید

---

## 📞 پشتیبانی

اگر مشکلی پیش آمد:

1. لاگ‌های برنامه را بررسی کنید: `pm2 logs`
2. لاگ‌های Nginx را بررسی کنید: `/var/log/nginx/error.log`
3. لاگ‌های سیستم را بررسی کنید: `journalctl -xe`

برای سوالات بیشتر، به `README.md` مراجعه کنید.

---

**موفق باشید! 🚀**

