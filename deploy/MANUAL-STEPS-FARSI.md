# 📝 دستورالعمل دستی دیپلوی iranche.com

## ⚠️ توجه مهم
اتصال خودکار به سرور با خطا مواجه شد. احتمالاً به دلایل زیر:
- پورت SSH (12111002) ممکن است اشتباه باشد - معمولاً پورت 22 است
- فایروال سرور اتصال را بلاک می‌کند
- IP یا اطلاعات سرور نیاز به بررسی دارد

لطفاً مراحل زیر را **دستی** انجام دهید:

---

## مرحله 1️⃣: بررسی اتصال SSH

ابتدا مطمئن شوید که می‌توانید به سرور متصل شوید.

### گزینه A: با PuTTY (توصیه می‌شود برای ویندوز)

1. **PuTTY را باز کنید**
2. در قسمت **Host Name**: `178.239.147.72` را وارد کنید
3. در قسمت **Port**: `12111002` را امتحان کنید (اگر کار نکرد، `22` را امتحان کنید)
4. روی **Open** کلیک کنید
5. با username `root` و password مورد نظر وارد شوید

### گزینه B: با PowerShell

```powershell
ssh -p 12111002 root@178.239.147.72
```

اگر کار نکرد، پورت 22 را امتحان کنید:

```powershell
ssh -p 22 root@178.239.147.72
```

یا حتی بدون تعیین پورت:

```powershell
ssh root@178.239.147.72
```

---

## مرحله 2️⃣: بعد از اتصال به سرور

وقتی به سرور متصل شدید، دستورات زیر را **یکی یکی** کپی و اجرا کنید:

### ✅ Step 1: نصب Git

```bash
dnf install -y git
```

### ✅ Step 2: کلون کردن پروژه

```bash
cd /root
git clone https://github.com/electera-top/iranche.git
cd iranche/deploy
chmod +x *.sh
```

### ✅ Step 3: نصب نیازمندی‌های سرور (5-10 دقیقه)

این مرحله Node.js, Nginx, PM2 و Certbot را نصب می‌کند:

```bash
./server-setup.sh
```

**صبر کنید تا تمام شود!** این مرحله چند دقیقه طول می‌کشد.

### ✅ Step 4: دیپلوی برنامه (3-5 دقیقه)

```bash
./deploy-app.sh
```

این مرحله:
- کد را کلون می‌کند
- Dependencies را نصب می‌کند (npm install)
- Build می‌گیرد (npm run build)
- با PM2 اجرا می‌کند

### ✅ Step 5: تنظیم Nginx

```bash
./setup-nginx.sh
```

### ✅ Step 6: بررسی وضعیت

```bash
# بررسی PM2
pm2 status

# بررسی لاگ‌ها
pm2 logs iranche-frontend

# تست محلی
curl http://localhost:3000

# بررسی Nginx
systemctl status nginx
```

اگر همه چیز درست باشد، باید پیام‌های OK و Running ببینید.

---

## مرحله 3️⃣: تست سایت

حالا سایت شما باید در آدرس زیر در دسترس باشد:

```
http://iranche.com
```

آن را در مرورگر باز کنید و بررسی کنید.

---

## مرحله 4️⃣: تنظیم SSL/HTTPS (اختیاری اما توصیه می‌شود)

⚠️ **قبل از این مرحله:**
- مطمئن شوید DNS دامنه به سرور اشاره می‌کند
- سایت در http://iranche.com کار می‌کند

برای بررسی DNS:

```bash
dig iranche.com
# یا
nslookup iranche.com
```

باید IP سرور شما (178.239.147.72) را نشان دهد.

اگر DNS درست است، SSL را نصب کنید:

```bash
cd /root/iranche/deploy
./setup-ssl.sh
```

زمانی که از email پرسید، یک email معتبر وارد کنید (برای اطلاع‌رسانی تمدید گواهی).

بعد از نصب SSL، سایت در آدرس زیر هم در دسترس خواهد بود:

```
https://iranche.com
```

---

## 🔍 عیب‌یابی

### برنامه اجرا نمی‌شود

```bash
# بررسی لاگ‌های PM2
pm2 logs iranche-frontend --err

# بررسی پورت 3000
netstat -tlnp | grep 3000

# اجرای دستی برای دیباگ
cd /var/www/iranche/public-frontend
npm run build
npm start
```

### Nginx خطا می‌دهد

```bash
# تست تنظیمات
nginx -t

# مشاهده خطاها
tail -f /var/log/nginx/error.log

# ری‌استارت
systemctl restart nginx
```

### پورت 3000 اشغال است

```bash
# پیدا کردن پروسس
lsof -i :3000

# کشتن پروسس
pm2 stop iranche-frontend
pm2 delete iranche-frontend
```

### DNS کار نمی‌کند

اگر DNS به سرور اشاره نمی‌کند:

1. به پنل DNS دامنه بروید
2. A record بسازید:
   - Type: `A`
   - Name: `@` (برای iranche.com)
   - Value: `178.239.147.72`
   - TTL: `3600`
3. یک A record دیگر برای www:
   - Type: `A`
   - Name: `www`
   - Value: `178.239.147.72`
   - TTL: `3600`

صبر کنید 1-24 ساعت تا DNS propagate شود.

---

## 🔄 دستورات مفید

### مدیریت برنامه

```bash
pm2 status                      # بررسی وضعیت
pm2 logs iranche-frontend       # مشاهده لاگ‌ها
pm2 restart iranche-frontend    # ری‌استارت
pm2 stop iranche-frontend       # توقف
pm2 monit                       # مانیتورینگ زنده
```

### به‌روزرسانی برنامه

هر وقت کد را در GitHub تغییر دادید:

```bash
cd /root/iranche/deploy
./deploy-app.sh
```

### مدیریت Nginx

```bash
systemctl status nginx          # وضعیت
systemctl restart nginx         # ری‌استارت
systemctl reload nginx          # reload تنظیمات
nginx -t                        # تست تنظیمات
```

### مشاهده لاگ‌ها

```bash
# لاگ‌های برنامه
pm2 logs iranche-frontend

# لاگ‌های Nginx
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log

# لاگ‌های سیستم
journalctl -xe
journalctl -u nginx -f
```

---

## 📊 بررسی استفاده منابع

```bash
# استفاده CPU و RAM
htop
# یا
top

# استفاده دیسک
df -h

# استفاده حافظه
free -h

# پروسس‌های Node.js
ps aux | grep node
```

---

## 🔒 توصیه‌های امنیتی

بعد از دیپلوی موفق، حتماً این کارها را انجام دهید:

### 1. تغییر پسورد root

```bash
passwd
```

### 2. ایجاد کاربر غیر root

```bash
adduser deploy
passwd deploy
usermod -aG wheel deploy
```

### 3. تنظیم SSH Key Authentication

در کامپیوتر خودتان (ویندوز):

```powershell
ssh-keygen -t rsa -b 4096
```

سپس کلید public را به سرور بفرستید:

```powershell
type $env:USERPROFILE\.ssh\id_rsa.pub | ssh root@178.239.147.72 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

### 4. غیرفعال کردن Password Authentication

در سرور:

```bash
nano /etc/ssh/sshd_config
```

تغییر دهید:

```
PasswordAuthentication no
```

سپس SSH را ری‌استارت کنید:

```bash
systemctl restart sshd
```

### 5. نصب fail2ban

```bash
dnf install -y fail2ban
systemctl enable fail2ban
systemctl start fail2ban
```

---

## 📦 پشتیبان‌گیری

### Backup دستی

```bash
# Backup کد
tar -czf /root/backup-iranche-$(date +%Y%m%d).tar.gz /var/www/iranche

# Backup تنظیمات Nginx
tar -czf /root/backup-nginx-$(date +%Y%m%d).tar.gz /etc/nginx/conf.d

# Backup SSL
tar -czf /root/backup-ssl-$(date +%Y%m%d).tar.gz /etc/letsencrypt
```

### Backup خودکار با Cron

```bash
crontab -e
```

اضافه کنید:

```cron
# Backup هر روز ساعت 2 صبح
0 2 * * * tar -czf /root/backup-iranche-$(date +\%Y\%m\%d).tar.gz /var/www/iranche
```

---

## ✅ چک‌لیست نهایی

- [ ] سرور به‌روزرسانی شده (dnf update)
- [ ] Git نصب شده
- [ ] Node.js 20.x نصب شده
- [ ] Nginx نصب و اجرا شده
- [ ] PM2 نصب و تنظیم شده
- [ ] کد از GitHub کلون شده
- [ ] برنامه build و deploy شده
- [ ] PM2 برنامه را اجرا می‌کند
- [ ] Nginx تنظیم شده
- [ ] سایت در http://iranche.com کار می‌کند
- [ ] DNS به سرور اشاره می‌کند
- [ ] SSL نصب شده (اختیاری)
- [ ] سایت در https://iranche.com کار می‌کند (اگر SSL نصب کردید)
- [ ] پسورد root تغییر کرده
- [ ] Backup تنظیم شده

---

## 📞 در صورت نیاز به کمک

اگر در هر مرحله به مشکل خوردید:

1. **لاگ‌ها را بررسی کنید:**
   ```bash
   pm2 logs iranche-frontend
   tail -f /var/log/nginx/error.log
   journalctl -xe
   ```

2. **وضعیت سرویس‌ها را چک کنید:**
   ```bash
   pm2 status
   systemctl status nginx
   ```

3. **دستورات عیب‌یابی را اجرا کنید:**
   ```bash
   # تست اتصال به برنامه
   curl http://localhost:3000
   
   # بررسی پورت‌ها
   netstat -tlnp | grep -E '3000|80|443'
   
   # بررسی پروسس‌ها
   ps aux | grep -E 'node|nginx'
   ```

---

**موفق باشید! 🚀**

اگر همه مراحل را درست انجام دادید، سایت شما الان باید کاملاً فعال باشد.

